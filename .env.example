# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# GitHub PAT is now a server-side secret on Supabase Edge Functions.
# Set it with: supabase secrets set GITHUB_PAT='github_pat_...'

# Google Drive (Optional for links)
# No specific key needed if just using public/shared links
